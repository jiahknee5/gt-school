# dependencies / build output
node_modules/
.next/
out/
dist/
coverage/

# env / secrets — NEVER commit (credential hygiene is scored)
.env
.env.local
.env.*.local
*.key
*.pem

# next.js / typescript generated
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.log
.vercel

# seed generator build artifact (regenerated by `npm run seed`)
.seed-build/
# opt-in SQL dump (regenerate with `npm run seed -- --sql`)
seed-data/load.sql
# ephemeral test report (regenerate with `npm run test:report`)
seed-data/test-results.json
